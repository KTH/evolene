# If pipenv fails with: TypeError: 'module' object is not callable
# Run: pipenv run pip install pip==18.0

[[source]]
url = "https://pypi.python.org/simple"
verify_ssl = true

[dev-packages]
green       = ">=3.3"
coverage    = ">=5.5"
mock        = ">=4.0"
pylint      = ">=2.8"
autopep8 = "*"

[packages]
requests    = ">=2.25"
coloredlogs = ">=15.0"
setuptools  = ">=56.0"

[requires]
python_version = "3"
